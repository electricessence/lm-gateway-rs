/target

# IDE / editor
.vscode/

# Local environment defaults (machine-specific — not committed)
tools/.env.ps1

# Temp scripts and test results (session-specific, not committed)
tools/temp/

# Live operational config — machine-specific, not committed
etc/lm-gateway/config.toml
etc/lm-gateway/env

# One-off test artifacts
ct.json
ct_full.json
run-live-test.sh